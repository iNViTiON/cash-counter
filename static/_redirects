# Deliberately empty. Read this before adding a catch-all back.
#
# `/*  /index.html  200` used to live here and was removed, but removing it
# changed NOTHING: the SPA fallback that caused the outage is Cloudflare Pages'
# own behaviour, not a redirect rule. Pages only stops guessing once a
# `404.html` exists, which is why `static/404.html` is the actual fix. That file
# carries the full explanation.
#
# There are no client-side routes anyway — `src/routes/+page.svelte` is the only
# one and it prerenders to `/index.html`. If real routes are ever added, scope a
# rewrite to those paths explicitly and never use `/*`, or a missing asset can
# be answered with the shell at status 200 and cached as immutable JavaScript.
